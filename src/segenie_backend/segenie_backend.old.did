type Badge = record {
    "id": nat;
    "name": text;
    "description": text;
    "creator": principal;
}

service : {
    "create_badge": (text, text) -> (text);
    "update_badge_metadata": (nat, text, text) -> (text);
    "get_badge": (nat) -> (Badge);
}
